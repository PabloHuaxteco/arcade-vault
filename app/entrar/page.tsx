// Auth — pantalla de acceso. Server Component que monta la isla Client
// <AuthForm>. Portado de references/templates/auth.jsx.

import { AuthForm } from "@/app/_components/auth-form";

export default function EntrarPage() {
  return <AuthForm />;
}
